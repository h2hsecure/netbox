syntax = "proto3";
option go_package = "internal/repository/grpc";

message UserIpTime {
  string ip = 1;
  string user = 2;
  string path = 3;
  int32  timestamp = 4;
}

message Empty {

}

service NetworkEvent {
   rpc Send(UserIpTime) returns (Empty) {}
}